/**
 * Single source of truth for the extension's version string.
 *
 * Updated automatically by `scripts/deploy.js` when bumping versions.
 * Do not edit by hand — run `npm run deploy` (or set in package.json,
 * manifest.json, static/version.json, and re-run the deploy).
 */
export const VERSION = '1.3.1';
